pluginManagement {
	pluginManagement {
		repositories {
			gradlePluginPortal()
			maven {
				name = "Fabric"
				url = "https://maven.fabricmc.net/"
			}
		}
	}
}

rootProject.name = 'tensai'

include("shared")

[
		"api",
		"fabric",
		"spigot",
].forEach {
	include("tensai-$it")
}
